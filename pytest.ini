[pytest]
testpaths = tests
pythonpath = src
addopts = --cov=tartarus_v2 --cov-config=.coveragerc --cov-report=term-missing
markers =
    gui: tests that require GTK4/Libadwaita (skip on Windows / no GI)
